import 'package:flutter/material.dart';

class Prendre extends StatelessWidget {
  const Prendre({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}